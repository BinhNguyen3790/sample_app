<% if object.errors.any? %>
  <div id="<%= t(".id") %>">
    <div class="<%= t(".class") %>">
      <%= t(".text") %> <%= pluralize(object.errors.size, t(".error")) %>
    </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
