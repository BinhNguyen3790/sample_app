<li>
  <%= gravatar_for user, size: Settings.views.users.user.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".delete"), user, method: :delete,
      data: { confirm: t(".text") } %>
  <% end %>
</li>
